- q: "Does `canon init` create `.canon/check.yml` from the default template file at `.canon/templates/default/check.yml` and fail without overwriting when `.canon/check.yml` already exists?"
  a: "yes"

- q: "Apart from the shebang, is the pre-commit hook only a direct invocation of `canon gate`?"
  a: "yes"

# - q: "Is the local Git configuration for this repository set to run the repository pre-commit hook?"
#   a: "yes"
#   cooldown: 7d
