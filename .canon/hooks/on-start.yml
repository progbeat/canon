- |-
    Run the checks:
    ```
    cargo fmt --check
    cargo clippy --all-targets -- -D warnings
    cargo test
    ```
    Consider `cargo build --release` or `cargo audit --deny warnings` when relevant.
- input: "Have the checks passed? [y/N] "
  cases:
    y: !ok
    _: !block "▷ Fix any failures, then run `canon check` again."
