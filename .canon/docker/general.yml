- q: |
    Does the Dockerfile set these container environment variables?
    - CANON_SECRET_DIR=/scratch/secret/
    - CANON_SANDBOX_DIR=/scratch/sandbox
    - CODEX_HOME=/scratch/codex-home
  a: "yes"

- q: "Does the Docker entrypoint leave Canon-owned command and argument validation to be handled by the `canon` binary?"
  a: "yes""

- q: "Does the Docker entrypoint force `canon check` invocations to run with `--no-sandbox` while forwarding all user-supplied arguments unchanged?"
  a: "yes"

- q: "Does the Docker entrypoint override Git object database environment variables?"
  a: "no"
