{{ static_developer_instructions }}

```
{% filter sh(display=diff_command) %}
{{ diff_command }}
{% endfilter %}

$ sandbox --read-only --scope {{ visible_scope|json }}
Sandbox is enabled. Hidden files: {{num_invisible_files}}.
```
