Response format:
Return exactly one valid JSON object, no prose or fences: {"answer":"<single-line answer>","evidence":"<evidence with backticked project-relative refs>","scope":["<normalized repo-relative path>"]}. Escape quotes, backslashes, and control characters inside JSON strings.
answer is only the exact yes/no token, option letter, exact requested single-line string, idk, or malformed; put explanation only in evidence. Evidence citations must be project-relative refs enclosed in backticks. Do not cite proxy evidence; cite the project files or code that directly support the answer.

Inspection policy:
- Prefer `rg` and `rg --files` for text and file searches; fall back cleanly if unavailable.
- Parallelize independent read-only inspections with `multi_tool_use.parallel` when practical, especially `cat`, `rg`, `sed`, `ls`, `git show`, `nl`, and `wc`.
- Keep shell output readable: avoid noisy chained separators such as `echo "====";`.
- Keep context tight: read only what is needed, avoid bulk-loading files, and expand searches only when current context is insufficient.

Answer-selection policy:
When the question asks for a yes/no answer and there is sufficient evidence, put exactly `yes` or `no` in the JSON `answer` field. When the question asks you to choose from lettered options, put exactly the option letter. When the question asks for another exact single-line answer, put only that exact answer string. Do not guess. If there is not enough evidence to answer a valid question, put exactly `idk` in `answer`. If the question is malformed, put exactly `malformed` in `answer`. Never include explanation in `answer`; put explanation only in `evidence`. Never output the raw answer as the whole response.

Scoring policy:
- Correct answer: +5
- Incorrect answer: -10
- `idk`: -1 + retry on full project scope
- `malformed`: N/A, human review required
- Successful scope narrowing: +1
- Failed scope narrowing: -5

Enforced scope: {{scope}}
