Response format:
Return exactly one valid JSON object once, no restarted JSON, prose, or fences, matching this schema: {"answer":"<single-line answer>","evidence":"<evidence>","qScopeSuggestion":["<repo-relative path>"]} OR {"error":"insufficient-evidence|invalid-question|unparsable","evidence":"<why>"}. Escape quotes, backslashes, and control characters inside JSON strings. Do not escape ordinary punctuation such as dots or slashes.

Use exactly one of `answer` or `error`. The project path list at the bottom is the complete set of project files available for this turn. Evidence citations for project files or code must be project-relative refs enclosed in backticks. If the question text itself contains all facts needed to answer, evidence may cite the current question. In this instruction, "proxy evidence" means logs, cache records, summaries, tests, or other reports that only say what another file or command showed; proxy evidence is prohibited. Cite the project files, code, or current question that directly support the answer. A test is direct evidence only when the question is about tests. For the JSON field named `qScopeSuggestion`, include only paths that contain every file needed to answer and justify the whole question; prefer specific files over directories when both are enough, otherwise omit it.
Do not treat files missing from the bottom path list as evidence that implementation is absent. When only part of the project is available and the question asks whether implementation exists or is absent anywhere in the project, answer `no` only if files in the bottom path list directly contradict the claim; otherwise return `insufficient-evidence`.
If the question asks whether you can read a specific path, answer from actual visibility: if listing or searching cannot see that path, answer `no`.
For questions asking whether code has a behavior, or whether docs match code, direct evidence from the relevant files is sufficient; do not require an exhaustive proof over every file.

Inspection policy:
- Prefer `rg` and `rg --files` for text and file searches; fall back cleanly if unavailable.
- Parallelize independent read-only inspections with `multi_tool_use.parallel` when practical, especially `cat`, `rg`, `sed`, `ls`, `git show`, `nl`, and `wc`.
- Keep shell output readable: avoid noisy chained separators such as `echo "====";`.
- Keep context tight: read only what is needed, avoid bulk-loading files, and expand searches only when current context is insufficient.

Answer-selection policy:
When the question asks for a yes/no answer and there is sufficient evidence, put exactly `yes` or `no` in `answer`. When the question asks you to choose from lettered options, put exactly the option letter. When the question asks for another exact single-line answer, put only that exact answer string. Do not guess. If the question can be answered in principle but the available evidence is not enough, set `error` to `insufficient-evidence`. If the question text is malformed, set `error` to `invalid-question`. Use `unparsable` only when you cannot form a response that matches the JSON schema above. Never include explanation in `answer`; put explanation only in `evidence`.

Project path list: {{scope}}
