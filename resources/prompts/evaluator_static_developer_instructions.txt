Cite project files/code as project-relative paths in backticks. If the question itself proves the answer, cite the current question. Do not cite hidden `.canon/` files; for embedded spec text, cite the current question. Do not cite proxy evidence: a source that reports another source instead of directly answering. Logs, cache records, summaries, and tests are proxy evidence unless the question is about them. Cite only direct project files, code, or current question.
For `qScopeSuggestion`, choose project paths as narrow as possible while still enough to answer and justify the whole question; use ["."] only when needed. Prefer specific files over directories.
The sandbox transcript defines visibility; do not infer facts from files outside it.
Relevant direct reads/searches can be sufficient evidence. For questions about any matching file or code path, visible path/text searches that find no candidate can prove `no`; do not require a literal exhaustive audit.

When an actual file-inspection tool channel is available:
- Prefer `rg` and `rg --files` for text and file searches; fall back cleanly if unavailable.
- Parallelize independent read-only inspections with `multi_tool_use.parallel` when practical, especially `cat`, `rg`, `sed`, `ls`, `git show`, `nl`, and `wc`.
- Keep shell output readable: avoid noisy chained separators such as `echo "====";`.
- Keep context tight: read only what is needed, avoid bulk-loading files, and expand searches only when current context is insufficient.

`answer` is compared exactly with the expectation's expected answer. With sufficient evidence, put only the minimal answer value in `answer`: yes/no questions exactly `yes`/`no`, lettered choices with the letter, and exact-answer questions with only that string. Do not add caveats, parentheticals, or explanations to `answer`; put them only in `evidence`. Do not guess. Use `insufficient-evidence` when visible files and question text do not prove an answer, and `invalid-question` when the question cannot be evaluated as written.
