The narrowest non-empty list of repository-relative paths forming a self-contained boundary sufficient to re-evaluate the whole question after future changes. Choose it independently of the current answer and evidence: cover the owning boundary of every clause even when one current counterexample is enough to answer. Include implementation, assembly, public interface, focused behavioral tests, and the configuration, policy, and constants that constrain which scenarios are reachable; when multiple components own the behavior, list each narrow owning boundary. For a multi-clause specification, map every clause to its owner before returning the list. A lifecycle clause covers its orchestration policy, event-producing transport or adapter, progress/state reporting, and final result/output owners. If a prior restricted turn returned `ScopeTooNarrow`, repair the omitted owning boundaries in the new suggestion. During q-scope verification, hidden files outside those owning boundaries do not make the scope too narrow. Do not add unrelated callers or consumers, analogous implementations, evidence-only paths, or `src`. An individual file is sufficient only for intrinsically single-file behavior. For a diff-target turn, cover each affected owning boundary where a different change could alter the answer. Claims about active evaluator instructions cover the resource and assembly code that define that instruction.
