Concrete facts that directly justify the answer. Every claim that current visible project behavior or content exists must cite its defining code/content with an exact current visible project-relative `path:line` or `path:start-end` reference. An absence claim needs no invented code reference, but must state the complete visible domain inspected and why that domain establishes absence. Do not infer hidden paths, abbreviate references, or use placeholders.
